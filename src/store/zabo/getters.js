const getters = {
  zaboes(state) {
    return state.zaboes;
  },
  zaboesPageCount(state) {
    return state.zaboesPageCount;
  },
  detailPageFrom(state) {
    return state.from;
  },
};

export default getters;
